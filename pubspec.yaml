name: app_tdd_exemplo
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: 8.0.1
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  hive: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: 9.0.2
  lint: 1.8.1
  flutter_lints: ^1.0.0
  mockito: ^5.1.0
  build_runner: ^2.1.2
flutter:
  uses-material-design: true
